name: action on a peer review

on: pull_request
  # pull_request:
    # branches:
    #   - master

jobs:
  action-on-a-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Do check
      - run: scripts/collabrator-check.sh